<html>
    <head>
        <title>String functions</title>
    </head>
    <body>
        @if(isset($message))
        {{$message}}
        @endif
        @if(isset($low))
        {{$low}}
        @endif
        @if(isset($entity))
        {{$entity}}
        @endif
    </body>
</html>